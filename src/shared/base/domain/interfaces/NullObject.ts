// Definimos la interfaz NullObject, que tendrá una propiedad isNull.
// Esta propiedad será utilizada para marcar objetos como "nulos" o vacíos en el sistema.

export default interface NullObject {
    isNull:  boolean // La propiedad isNull se utiliza para indicar si el objeto es nulo o vacío.
}